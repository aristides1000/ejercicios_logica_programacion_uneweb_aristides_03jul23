#include <stdio.h>

int main() {
  printf("Hola mundo");
  printf("Estoy aprendiendo lenguaje C");
  /*
    Podemos utilizar tantas funciones "printf()" como queramos. Sin embargo, tenemos que tener en cuenta que no se va a insertar una nueva linea al final de la salida.
  */
  return 0;
}